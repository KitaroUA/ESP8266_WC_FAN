var formatNum = function (num) { if (num.toString().length < 2) { return '0' + num; }    return num; };  var option_1 = function (vId,top_v,v_selected){ $('#'+vId).empty(); select = document.getElementById(vId); for (var i = 0; i<top_v; i++){ var opt = document.createElement('option'); opt.value = i; opt.innerHTML = i; select.appendChild(opt); } document.getElementById(vId).selectedIndex = v_selected; }
//Some comment to protect or world, Some comment to protect or world, Some comment to protect or world, Some comment to protect or world, Some comment to protect or world, Some comment to protect or world

